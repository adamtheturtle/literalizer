Every language now declares for itself what it once inherited from a default on the language metaclass, so nothing takes a behavior nobody considered for it. A meta-test holds every language to the list.
