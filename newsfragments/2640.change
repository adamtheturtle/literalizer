Add a Bash JSON round-trip check to CI using the shared round-trip fixture, driven by the pinned ``bash`` interpreter. Because Bash associative arrays cannot nest, the check walks the known JSON shape and ``eval``s each nested-collection string back into a fresh array before re-emitting JSON.
