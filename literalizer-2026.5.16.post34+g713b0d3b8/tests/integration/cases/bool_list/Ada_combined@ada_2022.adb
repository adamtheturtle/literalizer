with A_Stub; use A_Stub;
procedure Main is
    my_data : A_Val := AList'[
        ABool (True),
        ABool (False),
        ABool (True)
    ];
begin
    my_data := AList'[
        ABool (True),
        ABool (False),
        ABool (True)
    ];
end Main;
