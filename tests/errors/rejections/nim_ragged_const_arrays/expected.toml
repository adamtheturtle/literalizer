# languages rejecting: 1; languages accepting: 0

[UnrepresentableInputError]
Nim = "Nim renders a sequence as a fixed-size array, whose length is part of its type, so sibling sequences of lengths 1, 2 have no common type"
