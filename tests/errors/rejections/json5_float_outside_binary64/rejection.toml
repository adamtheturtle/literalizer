schema_version = 1
summary = "Finite JSON5 numbers outside binary64 range are rejected."
exceptions = ["ParseError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["1e309", "-1e-4000"]
input_format = "json5"
