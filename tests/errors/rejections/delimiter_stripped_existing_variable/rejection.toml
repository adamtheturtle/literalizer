schema_version = 1
summary = "A delimiter-stripped fragment cannot assign an existing variable."
exceptions = ["InvalidRenderArgumentError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["[1, 2]"]
input_format = "json"
include_delimiters = false
variable_form = "existing"
variable_name = "x"
