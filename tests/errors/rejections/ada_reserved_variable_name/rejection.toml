schema_version = 1
summary = "Ada reserved words that no declaration can name."
exceptions = ["ReservedVariableNameError"]
languages = ["Ada"]
values = ["abs", "and", "parallel", "some"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
