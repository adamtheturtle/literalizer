# languages rejecting: 1; languages accepting: 0

[UnrepresentableInputError]
"C['items:\\n- sub:\\n  - x: 1\\n- sub:\\n  - x: 2\\n  - x: 3\\n']" = "C cannot represent two record-shaped dicts of one shape whose shared all-record-list field has differing lengths under the RECORD heterogeneous strategy: that field becomes a fixed-size struct RecordN [len] member sized from the shape's first-seen instance, so a later instance with a different-length list would overflow the array (excess initializers fail to compile) or silently under-fill it -- neither faithfully represents the input (cf. the set / non-record-dict field boundary tracked in #2317)"
"C['- group:\\n  - x: 1\\n- group:\\n  - x: 1\\n  - x: 2\\n']" = "C cannot represent two record-shaped dicts of one shape whose shared all-record-list field has differing lengths under the RECORD heterogeneous strategy: that field becomes a fixed-size struct RecordN [len] member sized from the shape's first-seen instance, so a later instance with a different-length list would overflow the array (excess initializers fail to compile) or silently under-fill it -- neither faithfully represents the input (cf. the set / non-record-dict field boundary tracked in #2317)"
