schema_version = 1
summary = "An empty dict key has no form in these back ends."
exceptions = ["InvalidDictKeyError"]
languages = ["Dhall", "R"]

[call]
api = "literalize"
sources = ['{"": "value"}']
input_format = "json"
