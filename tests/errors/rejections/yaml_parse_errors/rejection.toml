schema_version = 1
summary = "Malformed YAML raises typed errors on safe and round-trip parse paths."
exceptions = ["YAMLParseError"]
languages = ["Python"]

[call]
api = "literalize"
sources = [":\n  :\n    - ][", "value: [1\n# force roundtrip path\n"]
input_format = "yaml"
