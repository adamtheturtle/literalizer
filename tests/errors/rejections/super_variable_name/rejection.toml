schema_version = 1
summary = "Haxe and Objective-C cannot use super as a variable name."
exceptions = ["ReservedVariableNameError"]
languages = ["Haxe", "ObjectiveC"]
values = ["super"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
