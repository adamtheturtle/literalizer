schema_version = 1
summary = "C++ rejects import as a wrapper module name."
exceptions = ["InvalidModuleNameError"]
languages = ["Cpp"]

[call]
api = "constructor"
kwargs = [{ kind = "text", kwarg = "module_name", value = "import" }]
