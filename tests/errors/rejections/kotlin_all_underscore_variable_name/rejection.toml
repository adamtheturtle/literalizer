schema_version = 1
summary = "Kotlin reserves every all-underscore name, not just the blank identifier."
exceptions = ["ReservedVariableNameError"]
languages = ["Kotlin"]
values = ["__", "___"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
