schema_version = 1
summary = "Complex YAML set keys are rejected as parse errors."
exceptions = ["YAMLParseError"]
languages = ["Mojo"]

[call]
api = "literalize"
sources = ["!!set\n? [a, b]\n"]
input_format = "yaml"
