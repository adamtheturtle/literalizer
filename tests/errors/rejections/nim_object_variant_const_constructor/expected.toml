# languages rejecting: 1; languages accepting: 0

[IncompatibleFormatsError]
Nim = "Nim CONST requires a constant-expression initializer, but OBJECT_VARIANT produces runtime .toTable / @[] calls which are not constant expressions. Use VAR or LET instead."
