schema_version = 1
summary = "A finite YAML number above binary64 range is rejected."
exceptions = ["ParseError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["1e309"]
input_format = "yaml"
