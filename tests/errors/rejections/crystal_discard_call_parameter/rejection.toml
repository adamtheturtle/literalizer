schema_version = 1
summary = "Crystal's discard marker cannot name a call parameter."
exceptions = ["InvalidCallParameterNameError"]
languages = ["Crystal"]

[call]
api = "literalize_call"
sources = ["[[1]]"]
input_format = "json"
target_function = "do_thing"
parameter_names = ["_"]
wrap_in_file = true
