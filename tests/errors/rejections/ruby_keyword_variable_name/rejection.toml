schema_version = 1
summary = "The Ruby pseudo-variables that cannot be assigned to."
exceptions = ["ReservedVariableNameError"]
languages = ["Ruby"]
values = ["__ENCODING__", "__FILE__", "__LINE__"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
