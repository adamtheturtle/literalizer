schema_version = 1
summary = "A control character reaching the numeric pre-scan is a YAML parse error."
exceptions = ["YAMLParseError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["e: 1 \u007f\n"]
input_format = "yaml"
