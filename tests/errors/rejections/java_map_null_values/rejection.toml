schema_version = 1
summary = "Java Map.entry() rejects null values."
exceptions = ["UnrepresentableInputError"]
languages = ["Java"]

[call]
api = "literalize"
sources = ['[{"a": {"b": null}}]', '{"a": null, "b": 1}']
input_format = "json"
