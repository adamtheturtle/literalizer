schema_version = 1
summary = "Crystal cannot bind data to the constructor name initialize."
exceptions = ["ReservedVariableNameError"]
languages = ["Crystal"]
values = [
  "initialize",
]

[call]
api = "literalize"
sources = ["{\"a\": 1, \"b\": [1, 2]}"]
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
