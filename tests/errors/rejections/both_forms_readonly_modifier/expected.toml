# languages rejecting: 1; languages accepting: 0

[ImmutableVariableModifierError]
CSharp = "CSharp cannot combine BothVariableForms with _CSharpModifiers.READONLY: the declaration binds the name once, so the assignment that follows it cannot compile"
