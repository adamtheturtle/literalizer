# languages rejecting: 1; languages accepting: 0

[UnrepresentableInputError]
"C['--- !!omap\\n- rec:\\n    a: 1\\n']" = "C cannot represent a record-shaped dict (or a list whose every element is one) nested under a non-record container -- a non-all-record list, a non-record dict, an ordered map, or a set -- under the RECORD heterogeneous strategy: it renders as a generated struct, which is not a CVal union member, so it is representable only as a record-field value, an element of such a field's struct array, or the document root (cf. the set / non-record-dict field boundary tracked in #2317)"
"C['field:\\n- - a: 1\\n']" = "C cannot represent a record-shaped dict (or a list whose every element is one) nested under a non-record container -- a non-all-record list, a non-record dict, an ordered map, or a set -- under the RECORD heterogeneous strategy: it renders as a generated struct, which is not a CVal union member, so it is representable only as a record-field value, an element of such a field's struct array, or the document root (cf. the set / non-record-dict field boundary tracked in #2317)"
"C['- - a: 1\\n']" = "C cannot represent a record-shaped dict (or a list whose every element is one) nested under a non-record container -- a non-all-record list, a non-record dict, an ordered map, or a set -- under the RECORD heterogeneous strategy: it renders as a generated struct, which is not a CVal union member, so it is representable only as a record-field value, an element of such a field's struct array, or the document root (cf. the set / non-record-dict field boundary tracked in #2317)"
