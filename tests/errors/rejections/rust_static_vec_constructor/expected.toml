# languages rejecting: 1; languages accepting: 0

[IncompatibleFormatsError]
Rust = "Rust STATIC requires a constant-expression initializer, but the VEC sequence format produces vec![…] which is not a constant expression. Use ARRAY or TUPLE instead."
