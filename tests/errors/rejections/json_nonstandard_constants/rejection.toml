schema_version = 1
summary = "Strict JSON rejects non-standard numeric constants."
exceptions = ["JSONParseError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["[NaN]", "[Infinity]", "[-Infinity]"]
input_format = "json"
