schema_version = 1
summary = "Malformed TOML raises a typed parse error."
exceptions = ["TOMLParseError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["not = [valid toml"]
input_format = "toml"
