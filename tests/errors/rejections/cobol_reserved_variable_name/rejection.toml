schema_version = 1
summary = "Cobol reserved words that no program name or data name can take."
exceptions = ["ReservedVariableNameError"]
languages = ["Cobol"]
values = ["function", "null"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
