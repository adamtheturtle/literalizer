# languages rejecting: 1; languages accepting: 0

[UnrepresentableInputError]
Rust = "Rust renders a sequence as a fixed-size array, whose length is part of its type, so sibling sequences of lengths 1, 3 have no common type"
