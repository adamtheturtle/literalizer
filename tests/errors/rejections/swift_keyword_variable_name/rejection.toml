schema_version = 1
summary = "``while`` cannot be used as a Swift identifier here."
exceptions = ["ReservedVariableNameError"]
languages = ["Swift"]
values = ["while"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
