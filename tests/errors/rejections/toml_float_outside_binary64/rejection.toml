schema_version = 1
summary = "Finite TOML numbers outside binary64 range are rejected."
exceptions = ["ParseError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["value = 1e309", "value = -1e-4000"]
input_format = "toml"
