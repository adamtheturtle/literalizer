schema_version = 1
summary = "C record fields cannot use reserved keywords."
exceptions = ["UnrepresentableInputError"]
languages = ["C"]

[call]
api = "literalize"
sources = ['[{"int": 1}]']
input_format = "json"
kwargs = [
  { kind = "option_member", option = "heterogeneous_strategy", member = "RECORD" },
]
