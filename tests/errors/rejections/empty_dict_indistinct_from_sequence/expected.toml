# languages rejecting: 7; languages accepting: 0

[UnrepresentableEmptyDictError]
"Ada['{}']" = "Ada cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Ada['{\"outer\": {}}']" = "Ada cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Ada['[{\"k\": 1}, {}]']" = "Ada cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Cobol['{}']" = ""
"Cobol['{\"outer\": {}}']" = ""
"Cobol['[{\"k\": 1}, {}]']" = ""
"CommonLisp['{}']" = "CommonLisp cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"CommonLisp['{\"outer\": {}}']" = "CommonLisp cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"CommonLisp['[{\"k\": 1}, {}]']" = "CommonLisp cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Lua['{}']" = "Lua cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Lua['{\"outer\": {}}']" = "Lua cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Lua['[{\"k\": 1}, {}]']" = "Lua cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Php['{}']" = "PHP cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Php['{\"outer\": {}}']" = "PHP cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Php['[{\"k\": 1}, {}]']" = "PHP cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"R['{}']" = "R cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"R['{\"outer\": {}}']" = "R cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"R['[{\"k\": 1}, {}]']" = "R cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Scheme['{}']" = "Scheme cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Scheme['{\"outer\": {}}']" = "Scheme cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
"Scheme['[{\"k\": 1}, {}]']" = "Scheme cannot represent an empty dict distinctly from an empty list; both serialize to the same runtime collection so the mapping/sequence distinction is lost on round-trip."
