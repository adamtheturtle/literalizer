schema_version = 1
summary = "Call comments reject embedded NUL bytes."
exceptions = ["CommentSourceNulError"]
languages = ["Python"]

[call]
api = "literalize_call"
sources = ["[[1]]"]
input_format = "json"
target_function = "process"
parameter_names = ["x"]
comment_source = ["before\u0000after"]
wrap_in_file = true
