schema_version = 1
summary = "``enum`` is a future reserved word in JavaScript, as it is in TypeScript."
exceptions = ["ReservedVariableNameError"]
languages = ["JavaScript"]
values = ["enum"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
