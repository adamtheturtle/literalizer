schema_version = 1
summary = "Python record fields cannot use reserved keywords."
exceptions = ["UnrepresentableInputError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ['[{"if": 1}]']
input_format = "json"
kwargs = [
  { kind = "option_member", option = "heterogeneous_strategy", member = "RECORD" },
]
