schema_version = 1
summary = "Rust keywords reserved for future use, which need a raw identifier."
exceptions = ["ReservedVariableNameError"]
languages = ["Rust"]
values = ["do", "final"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
