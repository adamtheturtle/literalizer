schema_version = 1
summary = "Elm exposing keyword cannot be used as a variable name."
exceptions = ["ReservedVariableNameError"]
languages = ["Elm"]
values = [
  "exposing",
]

[call]
api = "literalize"
sources = ["{\"a\": 1}"]
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
