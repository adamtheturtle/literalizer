schema_version = 1
summary = "Crystal rejects enum as a wrapper module name."
exceptions = ["InvalidModuleNameError"]
languages = ["Crystal"]

[call]
api = "constructor"
kwargs = [{ kind = "text", kwarg = "module_name", value = "enum" }]
