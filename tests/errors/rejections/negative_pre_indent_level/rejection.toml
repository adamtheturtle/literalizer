schema_version = 1
summary = "Negative pre-indent levels are invalid public render arguments."
exceptions = ["InvalidRenderArgumentError"]
languages = ["Python"]

[call]
api = "literalize"
sources = ["{\"a\": 1}"]
input_format = "json"
pre_indent_level = -1
