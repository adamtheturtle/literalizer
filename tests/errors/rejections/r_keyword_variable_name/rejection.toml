schema_version = 1
summary = "The typed ``NA`` constants of R are parser literals, not assignable names."
exceptions = ["ReservedVariableNameError"]
languages = ["R"]
values = ["NA_character_", "NA_complex_", "NA_integer_", "NA_real_"]

[call]
api = "literalize"
sources = ['{"a": 1}']
input_format = "json"
wrap_in_file = true
variable_form = "new"
variable_name = "{value}"
