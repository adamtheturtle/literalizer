import std.json;
void main() {
auto my_data = JSONValue([
    JSONValue("100% done"),
    JSONValue("%(name) is here"),
]);
my_data = JSONValue([
    JSONValue("100% done"),
    JSONValue("%(name) is here"),
]);
}
