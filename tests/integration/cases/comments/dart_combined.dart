void _declaration() {
  final my_data = {
      // Server configuration
      "host": "localhost",  // default host
      "port": 8080,
      // Enable debug mode
      "debug": true,
  };
}
void _assignment() {
  dynamic my_data;
  my_data = {
      // Server configuration
      "host": "localhost",  // default host
      "port": 8080,
      // Enable debug mode
      "debug": true,
  };
}
