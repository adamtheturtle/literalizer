#import <Foundation/Foundation.h>
int main(void) {
@autoreleasepool {
id my_data = @{
    // Server configuration
    @"host": @"localhost",  // default host
    @"port": @8080,
    // Enable debug mode
    @"debug": @YES,
};
(void)my_data;
my_data = @{
    // Server configuration
    @"host": @"localhost",  // default host
    @"port": @8080,
    // Enable debug mode
    @"debug": @YES,
};
    (void)my_data;
}
    return 0;
}
