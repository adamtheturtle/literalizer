let my_data = {
  -- Server configuration
  host = "localhost",  -- default host
  port = 8080,
  -- Enable debug mode
  debug = True,
} in my_data
let my_data = {
  -- Server configuration
  host = "localhost",  -- default host
  port = 8080,
  -- Enable debug mode
  debug = True,
} in my_data
