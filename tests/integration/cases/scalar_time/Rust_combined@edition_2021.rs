use std::collections::HashMap;
fn main() {
    let mut my_data = HashMap::from([
        ("starts_at", "09:30:00"),
    ]);
    my_data = HashMap::from([
        ("starts_at", "09:30:00"),
    ]);
    let _ = my_data;
}
