import std.json;
void main() {
auto my_data = JSONValue([
    // first
    JSONValue("a"),
    // second
    JSONValue("b"),
]);
my_data = JSONValue([
    // first
    JSONValue("a"),
    // second
    JSONValue("b"),
]);
}
