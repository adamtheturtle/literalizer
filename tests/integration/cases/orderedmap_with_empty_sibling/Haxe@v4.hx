class Fixture_orderedmap_with_empty_sibling_Haxe {
    public static function main() {
        final my_data = ([
            (["a" => 1] : Map<String, Dynamic>),
            ([] : Array<Dynamic>),
        ] : Array<Dynamic>);
    }
}
