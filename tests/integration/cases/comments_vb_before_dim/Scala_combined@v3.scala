object Fixture_comments_vb_before_dim_Scala_combined {
var my_data = Map(
    // Configuration
    "name" -> "app",
    // Port setting
    "port" -> 3000,
)
my_data = Map(
    // Configuration
    "name" -> "app",
    // Port setting
    "port" -> 3000,
)
}
