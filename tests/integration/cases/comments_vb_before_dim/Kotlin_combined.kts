fun _declaration() {
    val my_data = mapOf<String, Any?>(
        // Configuration
        "name" to "app",
        // Port setting
        "port" to 3000,
    )
}
fun _assignment() {
    var my_data: Any? = null
    my_data = mapOf<String, Any?>(
        // Configuration
        "name" to "app",
        // Port setting
        "port" to 3000,
    )
}
