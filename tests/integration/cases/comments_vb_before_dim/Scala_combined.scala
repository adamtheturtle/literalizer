object Declaration {
  val my_data = Map(
      // Configuration
      "name" -> "app",
      // Port setting
      "port" -> 3000,
  )
}
object Assignment {
  var my_data: Any = null
  my_data = Map(
      // Configuration
      "name" -> "app",
      // Port setting
      "port" -> 3000,
  )
}
