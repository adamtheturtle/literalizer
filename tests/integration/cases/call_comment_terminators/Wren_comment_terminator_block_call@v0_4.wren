class Process_ {
    construct new() {}
    call(value) {}
}
var process = Process_.new()
process.call("Dune")  /* first: * / |# -} *) ) =# ]] %} ]# % #> */
process.call("Solaris")
process.call("Neuromancer")  /* third: * / |# -} *) ) =# ]] %} ]# % #> */
