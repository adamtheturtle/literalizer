import std.json;
void main() {
int process(T...)(T args) { return 0; }
process("Dune");  /* first: * / |# -} *) ) =# ]] %} ]# % #> */
process("Solaris");
process("Neuromancer");  /* third: * / |# -} *) ) =# ]] %} ]# % #> */
}
