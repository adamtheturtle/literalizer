using System;
class Check {
static object process(object value = null) => null;
    public static void Main() {
process("Dune");  /* first: * / |# -} *) ) =# ]] %} ]# % #> */
process("Solaris");
process("Neuromancer");  /* third: * / |# -} *) ) =# ]] %} ]# % #> */
    }
}
