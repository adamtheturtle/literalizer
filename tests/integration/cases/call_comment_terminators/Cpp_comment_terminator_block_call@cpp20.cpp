#include <initializer_list>
#include <string>
#include <vector>
auto process(auto...) { return 0; }
int main() {
process("Dune");  /* first: * / |# -} *) ) =# ]] %} ]# % #> */
process("Solaris");
process("Neuromancer");  /* third: * / |# -} *) ) =# ]] %} ]# % #> */
    return 0;
}
