schema_version = 1
owner = "literalize-call"

[call]
target_function = "process"
parameter_names = ["value"]
per_element = true
comment_source = ["first: */ |# -} *) ) =# ]] %} ]# % #>", "", "third: */ |# -} *) ) =# ]] %} ]# % #>"]
requires_standalone_wrapped_comments = true
variant_only = true

[[call.variants]]
axis = "comment_terminator_call"
