class Fixture_yaml_tagged_scalars_Haxe_yaml_tagged_scalars {
    public static function main() {
        final my_data = ([
            "explicit_string" => "5",
            "six" => "explicitly tagged key",
        ] : Map<String, Dynamic>);
    }
}
