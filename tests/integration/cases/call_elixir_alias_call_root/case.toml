schema_version = 1
owner = "literalize-call"

[call]
target_function = "Playlist.new"
parameter_names = ["x"]
per_element = true
wrap_in_file = true
languages = ["Elixir"]
languages_reason = "Elixir alone treats a capitalized call root as an alias, which a generated stub names directly rather than binding."
