# Outdented from the inner mapping too, so the root claims this.
set my_data [dict create \
    "a" [dict create "b" [list 1] "c" 2] \
    "d" 3 \
]
# Outdented from the inner mapping too, so the root claims this.
set my_data [dict create \
    "a" [dict create "b" [list 1] "c" 2] \
    "d" 3 \
]
