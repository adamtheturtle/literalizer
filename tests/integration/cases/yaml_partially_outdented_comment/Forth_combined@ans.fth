\ Outdented from the inner mapping too, so the root claims this.
: my_data
+obj
    s\" a" +key +obj s\" b" +key +arr 1 +int -arr s\" c" +key 2 +int -obj
    s\" d" +key 3 +int
 -obj
;
\ Outdented from the inner mapping too, so the root claims this.
: my_data
+obj
    s\" a" +key +obj s\" b" +key +arr 1 +int -arr s\" c" +key 2 +int -obj
    s\" d" +key 3 +int
 -obj
;
