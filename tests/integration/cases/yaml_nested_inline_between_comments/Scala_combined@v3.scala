object Fixture_yaml_nested_inline_between_comments_Scala_combined {
var my_data = List(
    List(2, "hello"),  // trailing note
    // next element
    List(3, "world"),
)
my_data = List(
    List(2, "hello"),  // trailing note
    // next element
    List(3, "world"),
)
}
