class Fixture_nested_mixed_inner_Haxe {
    public static function main() {
        final my_data = ([
            ([1, "a"] : Array<Dynamic>),
            ([2, "b"] : Array<Dynamic>),
        ] : Array<Dynamic>);
    }
}
