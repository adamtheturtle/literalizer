(define my_data (list
    (cons "name" "Alice")
    (cons "scores" (list
        ; score meaning
        (cons 1 "first")
        (cons 2 "second")  ; latest score
    ))
))
(set! my_data (list
    (cons "name" "Alice")
    (cons "scores" (list
        ; score meaning
        (cons 1 "first")
        (cons 2 "second")  ; latest score
    ))
))
