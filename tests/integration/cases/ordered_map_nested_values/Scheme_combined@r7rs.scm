(define my_data (list
    (cons "name" "Alice")
    ; score collection
    (cons "scores" (list
        ; score meaning
        (cons 1 "first")
        (cons 2 "second")  ; latest score
    ))
))
(set! my_data (list
    (cons "name" "Alice")
    ; score collection
    (cons "scores" (list
        ; score meaning
        (cons 1 "first")
        (cons 2 "second")  ; latest score
    ))
))
