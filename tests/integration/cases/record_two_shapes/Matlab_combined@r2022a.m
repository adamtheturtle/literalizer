my_data = struct(
    'user', struct('id', 1, 'name', "Alice"),
    'project', struct('title', "report", 'tags', {{"draft", "urgent"}})
);
my_data = struct(
    'user', struct('id', 1, 'name', "Alice"),
    'project', struct('title', "report", 'tags', {{"draft", "urgent"}})
);
