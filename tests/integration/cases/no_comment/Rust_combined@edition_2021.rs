use std::collections::HashMap;
fn main() {
    let mut my_data = HashMap::from([
        ("message", "no comment here"),
    ]);
    my_data = HashMap::from([
        ("message", "no comment here"),
    ]);
    let _ = my_data;
}
