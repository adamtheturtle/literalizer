(define my_data (list
    (list (cons "item" "existing"))
    ; This comment describes the next item.
    (list (cons "item" "next"))
))
(set! my_data (list
    (list (cons "item" "existing"))
    ; This comment describes the next item.
    (list (cons "item" "next"))
))
