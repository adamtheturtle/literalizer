(define my_data (list
    (list "item" "existing")
    ; This comment describes the next item.
    (list "item" "next")
))
(set! my_data (list
    (list "item" "existing")
    ; This comment describes the next item.
    (list "item" "next")
))
