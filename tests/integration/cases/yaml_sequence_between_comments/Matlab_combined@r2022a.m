my_data = {
    struct('item', "existing"),
    % This comment describes the next item.
    struct('item', "next")
};
my_data = {
    struct('item', "existing"),
    % This comment describes the next item.
    struct('item', "next")
};
