use std::collections::HashMap;
fn main() {
    let mut my_data = HashMap::from([
        ("$ref", "my_var"),
    ]);
    my_data = HashMap::from([
        ("$ref", "my_var"),
    ]);
    let _ = my_data;
}
