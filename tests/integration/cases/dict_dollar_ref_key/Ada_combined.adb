with A_Stub; use A_Stub;
procedure Main is
    my_data : A_Val := AMap'[
        AEntry ("$ref", AStr ("my_var"))
    ];
begin
    my_data := AMap'[
        AEntry ("$ref", AStr ("my_var"))
    ];
end Main;
