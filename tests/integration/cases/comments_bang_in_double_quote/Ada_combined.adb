with A_Stub; use A_Stub;
procedure Main is
    my_data : A_Val := AMap'[
        AEntry ("key", AStr ("""bang!"""))  -- real
    ];
begin
    my_data := AMap'[
        AEntry ("key", AStr ("""bang!"""))  -- real
    ];
end Main;
