my_data = struct(
    'host', "localhost",
    'port', [],  % not configured yet
    'debug', true
);
my_data = struct(
    'host', "localhost",
    'port', [],  % not configured yet
    'debug', true
);
