my_data <- list(
    "host" = "localhost",
    "port" = NULL,  # not configured yet
    "debug" = TRUE
)
my_data <- list(
    "host" = "localhost",
    "port" = NULL,  # not configured yet
    "debug" = TRUE
)
