use JSON::PP;
my $my_data = {
    "host" => "localhost",
    "port" => undef,  # not configured yet
    "debug" => JSON::PP::true,
};
$my_data = {
    "host" => "localhost",
    "port" => undef,  # not configured yet
    "debug" => JSON::PP::true,
};
