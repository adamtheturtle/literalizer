(defparameter *my_data* (list
    (cons "host" "localhost")
    (cons "port" nil)  ; not configured yet
    (cons "debug" t)
))
(setf *my_data* (list
    (cons "host" "localhost")
    (cons "port" nil)  ; not configured yet
    (cons "debug" t)
))
