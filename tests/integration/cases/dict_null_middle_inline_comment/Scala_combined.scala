object Check {
var my_data = Map(
    "host" -> "localhost",
    "port" -> null,  // not configured yet
    "debug" -> true,
)
my_data = Map(
    "host" -> "localhost",
    "port" -> null,  // not configured yet
    "debug" -> true,
)
}
