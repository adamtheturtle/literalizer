from __future__ import annotations
my_data = {
    "host": "localhost",
    "port": None,  # not configured yet
    "debug": True,
}
my_data = {
    "host": "localhost",
    "port": None,  # not configured yet
    "debug": True,
}
