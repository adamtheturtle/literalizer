#lang racket
(define my_data (hash
    "host" "localhost"
    "port" (void)  ; not configured yet
    "debug" #t
))
(set! my_data (hash
    "host" "localhost"
    "port" (void)  ; not configured yet
    "debug" #t
))
