(define my_data (list
    (cons "host" "localhost")
    (cons "port" '())  ; not configured yet
    (cons "debug" #t)
))
(set! my_data (list
    (cons "host" "localhost")
    (cons "port" '())  ; not configured yet
    (cons "debug" #t)
))
