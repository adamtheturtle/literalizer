class Fixture_dict_with_homogeneous_annotated_values_Haxe {
    public static function main() {
        final my_data = ([
            "a" => ([] : Array<Dynamic>),
            "b" => ([] : Array<Dynamic>),
        ] : Map<String, Dynamic>);
    }
}
