import std.json;
void main() {
auto my_data = JSONValue([
    JSONValue("issue #{42}"),
    JSONValue("color #red"),
]);
my_data = JSONValue([
    JSONValue("issue #{42}"),
    JSONValue("color #red"),
]);
}
