schema_version = 1
languages = ["Rust"]
languages_reason = "Rust-specific: only its LAZY_STATIC declarations carry an inferred annotation, and the empty map is typed from its non-empty sibling."
suites = ["base"]

[[variants]]
axis = "sequence"

[[variants]]
axis = "declaration_style"
