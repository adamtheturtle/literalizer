(defparameter *my_data* (list
    (cons "collection" "alpha")
    (cons "featured_entry" (list (cons "id" 100) (cons "label" "first entry") (cons "enabled" nil) (cons "related_ids" (list 102 103))))
))
(setf *my_data* (list
    (cons "collection" "alpha")
    (cons "featured_entry" (list (cons "id" 100) (cons "label" "first entry") (cons "enabled" nil) (cons "related_ids" (list 102 103))))
))
