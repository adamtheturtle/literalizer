my_data = Dict(
    # About the first dotted key.
    # About the second dotted key.
    "dotted" => Dict("first" => 1, "second" => 2),
    "plain" => 3,  # About the plain key.
    # Inside the table.
    "table" => Dict("inner" => 4),
    # Before the first entry.
    # Before the second entry.
    "entries" => [Dict("name" => "one"), Dict("name" => "two")],
)
my_data = Dict(
    # About the first dotted key.
    # About the second dotted key.
    "dotted" => Dict("first" => 1, "second" => 2),
    "plain" => 3,  # About the plain key.
    # Inside the table.
    "table" => Dict("inner" => 4),
    # Before the first entry.
    # Before the second entry.
    "entries" => [Dict("name" => "one"), Dict("name" => "two")],
)
