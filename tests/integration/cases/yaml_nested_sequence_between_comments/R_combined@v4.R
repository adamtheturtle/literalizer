my_data <- list(
    list(
        list("item" = "existing"),
        "kept"
        # This comment trails the first pair.
    ),
    list(list("item" = "next"), "also kept"),
    # This comment describes the last pair.
    list(list("item" = "last"), "kept too")
)
my_data <- list(
    list(
        list("item" = "existing"),
        "kept"
        # This comment trails the first pair.
    ),
    list(list("item" = "next"), "also kept"),
    # This comment describes the last pair.
    list(list("item" = "last"), "kept too")
)
