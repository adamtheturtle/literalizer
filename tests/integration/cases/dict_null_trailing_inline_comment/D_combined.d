import std.json;
void main() {
auto my_data = JSONValue([
    "host": JSONValue("localhost"),
    "port": JSONValue(null),  // not configured yet
]);
my_data = JSONValue([
    "host": JSONValue("localhost"),
    "port": JSONValue(null),  // not configured yet
]);
}
