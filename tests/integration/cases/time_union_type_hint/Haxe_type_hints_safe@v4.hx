class Fixture_time_union_type_hint_Haxe_type_hints_safe {
    public static function main() {
        final my_data = ([
            "mixed" => ([(["09:30:00"] : Array<Dynamic>), ([] : Array<Dynamic>)] : Array<Dynamic>),
        ] : Map<String, Dynamic>);
    }
}
