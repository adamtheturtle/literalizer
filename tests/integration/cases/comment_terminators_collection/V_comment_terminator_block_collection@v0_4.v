
fn main() {
	my_data := [
		/* before first: * / |# -} *) (* ) =# ]] %} ]# % #> */
		'first',  /* inline first: * / |# -} *) (* ) =# ]] %} ]# % #> */
		/* before second: * / |# -} *) (* ) =# ]] %} ]# % #> */
		'second',
		/* trailing: * / |# -} *) (* ) =# ]] %} ]# % #> */
	]
	_ = my_data
}
