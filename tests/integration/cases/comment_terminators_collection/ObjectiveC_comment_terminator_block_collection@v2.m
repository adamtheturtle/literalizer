#import <Foundation/Foundation.h>
int main(void) {
@autoreleasepool {
id my_data = @[
    /* before first: * / |# -} *) ) =# ]] %} ]# % #> */
    @"first",  /* inline first: * / |# -} *) ) =# ]] %} ]# % #> */
    /* before second: * / |# -} *) ) =# ]] %} ]# % #> */
    @"second",
    /* trailing: * / |# -} *) ) =# ]] %} ]# % #> */
];
    (void)my_data;
}
    return 0;
}
