module Check = struct

type val_t =
  | OStr of string
  | OList of val_t list
let my_data : val_t = OList [
    (* before first: */ |# -} * ) ) =# ]] %} ]# % #> *)
    OStr "first";  (* inline first: */ |# -} * ) ) =# ]] %} ]# % #> *)
    (* before second: */ |# -} * ) ) =# ]] %} ]# % #> *)
    OStr "second"
    (* trailing: */ |# -} * ) ) =# ]] %} ]# % #> *)
]

end
