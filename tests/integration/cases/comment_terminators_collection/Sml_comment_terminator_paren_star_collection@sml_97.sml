datatype val_t =
    SStr of string
  | SList of val_t list
val my_data : val_t = SList [
    (* before first: */ |# -} * ) ( * ) =# ]] %} ]# % #> *)
    SStr "first",  (* inline first: */ |# -} * ) ( * ) =# ]] %} ]# % #> *)
    (* before second: */ |# -} * ) ( * ) =# ]] %} ]# % #> *)
    SStr "second"
    (* trailing: */ |# -} * ) ( * ) =# ]] %} ]# % #> *)
]
val _ = my_data
