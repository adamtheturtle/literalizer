fn main() {
    let my_data = vec![
        /* before first: * / |# -} *) (* ) =# ]] %} ]# % #> */
        "first",  /* inline first: * / |# -} *) (* ) =# ]] %} ]# % #> */
        /* before second: * / |# -} *) (* ) =# ]] %} ]# % #> */
        "second",
        /* trailing: * / |# -} *) (* ) =# ]] %} ]# % #> */
    ];
    let _ = my_data;
}
