object Fixture_comment_terminators_collection_Scala_comment_terminator_block_collection {
val my_data = List[String](
    /* before first: * / |# -} *) ) =# ]] %} ]# % #> */
    "first",  /* inline first: * / |# -} *) ) =# ]] %} ]# % #> */
    /* before second: * / |# -} *) ) =# ]] %} ]# % #> */
    "second",
    /* trailing: * / |# -} *) ) =# ]] %} ]# % #> */
)
}
