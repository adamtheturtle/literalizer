class Main {
    public static void main() {
var my_data = new String[]{
    /* before first: * / |# -} *) (* ) =# ]] %} ]# % #> */
    "first",  /* inline first: * / |# -} *) (* ) =# ]] %} ]# % #> */
    /* before second: * / |# -} *) (* ) =# ]] %} ]# % #> */
    "second"
    /* trailing: * / |# -} *) (* ) =# ]] %} ]# % #> */
};
    }
}
