let my_data: Any = [
    /* before first: * / |# -} *) ) =# ]] %} ]# % #> */
    "first",  /* inline first: * / |# -} *) ) =# ]] %} ]# % #> */
    /* before second: * / |# -} *) ) =# ]] %} ]# % #> */
    "second",
    /* trailing: * / |# -} *) ) =# ]] %} ]# % #> */
]
