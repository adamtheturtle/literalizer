package main

func main() {
my_data := []string{
	/* before first: * / |# -} *) (* ) =# ]] %} ]# % #> */
	"first",  /* inline first: * / |# -} *) (* ) =# ]] %} ]# % #> */
	/* before second: * / |# -} *) (* ) =# ]] %} ]# % #> */
	"second",
	/* trailing: * / |# -} *) (* ) =# ]] %} ]# % #> */
}
_ = my_data
}
