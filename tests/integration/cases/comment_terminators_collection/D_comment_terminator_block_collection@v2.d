import std.json;
void main() {
auto my_data = JSONValue([
    /* before first: * / |# -} *) (* ) =# ]] %} ]# % #> */
    JSONValue("first"),  /* inline first: * / |# -} *) (* ) =# ]] %} ]# % #> */
    /* before second: * / |# -} *) (* ) =# ]] %} ]# % #> */
    JSONValue("second"),
    /* trailing: * / |# -} *) (* ) =# ]] %} ]# % #> */
]);
}
