#+feature dynamic-literals
package main

main :: proc() {
my_data := [dynamic]any{
	/* before first: * / |# -} *) (* ) =# ]] %} ]# % #> */
	"first",  /* inline first: * / |# -} *) (* ) =# ]] %} ]# % #> */
	/* before second: * / |# -} *) (* ) =# ]] %} ]# % #> */
	"second",
	/* trailing: * / |# -} *) (* ) =# ]] %} ]# % #> */
}
_ = my_data
}
