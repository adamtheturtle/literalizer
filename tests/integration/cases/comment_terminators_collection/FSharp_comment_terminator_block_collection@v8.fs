module Main

type Val =
    | FStr of string
    | FList of Val list
let my_data: Val = FList [
    (* before first: */ |# -} * ) ) =# ]] %} ]# % #> *)
    FStr "first";  (* inline first: */ |# -} * ) ) =# ]] %} ]# % #> *)
    (* before second: */ |# -} * ) ) =# ]] %} ]# % #> *)
    FStr "second"
    (* trailing: */ |# -} * ) ) =# ]] %} ]# % #> *)
]
