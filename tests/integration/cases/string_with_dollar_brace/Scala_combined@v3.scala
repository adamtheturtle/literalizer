object Fixture_string_with_dollar_brace_Scala_combined {
var my_data = List[String](
    "prefix ${HOME} suffix",
    "${interpolated}",
)
my_data = List[String](
    "prefix ${HOME} suffix",
    "${interpolated}",
)
}
