object Fixture_yaml_unread_comment_slots_Scala_combined {
var my_data = Map(
    "flow" -> List[Int](
        1,
        // After the first element.
        2,
    ),
    // Between the key and its value.
    "gap" -> 3,
    // On the block scalar header.
    "block" -> "Text.\n",
    "anchored" -> 4,
    "alias" -> 4,
    // On the alias.
)
my_data = Map(
    "flow" -> List[Int](
        1,
        // After the first element.
        2,
    ),
    // Between the key and its value.
    "gap" -> 3,
    // On the block scalar header.
    "block" -> "Text.\n",
    "anchored" -> 4,
    "alias" -> 4,
    // On the alias.
)
}
