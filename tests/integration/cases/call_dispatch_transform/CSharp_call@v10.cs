using System;
class Check {
static object record_value(object value = null) => null;
static object flush_buffer(object count = null) => null;
static object emit(object _arg = null) => null;
    public static void Main() {
emit(record_value(42));
flush_buffer(3);
    }
}
