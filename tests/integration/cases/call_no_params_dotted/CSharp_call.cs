using System;
class Check {
class ThrottlerType_ { public object check() => null; }
static ThrottlerType_ throttler = new ThrottlerType_();
    public static void Main() {
throttler.check();
throttler.check();
    }
}
