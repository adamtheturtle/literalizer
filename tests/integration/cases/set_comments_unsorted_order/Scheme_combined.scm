(define my_data (list
    ; before apple
    "apple"
    "banana"  ; banana inline
    ; trailing
))
(set! my_data (list
    ; before apple
    "apple"
    "banana"  ; banana inline
    ; trailing
))
