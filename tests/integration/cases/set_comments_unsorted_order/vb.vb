Imports System.Collections.Generic
Module Check
    Dim x As Object = New HashSet(Of Object) From {
        ' before apple
        "apple",
        "banana"  ' banana inline
        ' trailing
    }
End Module
