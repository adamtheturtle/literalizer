var my_data = new HashSet<string> {
    // before apple
    "apple",
    "banana"  // banana inline
    // trailing
};
my_data = new HashSet<string> {
    // before apple
    "apple",
    "banana"  // banana inline
    // trailing
};
