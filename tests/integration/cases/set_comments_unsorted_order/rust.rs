use std::collections::HashMap;
use std::collections::HashSet;
fn main() {
    let _ = HashSet::from([
        // before apple
        "apple",
        "banana",  // banana inline
        // trailing
    ]);
}
