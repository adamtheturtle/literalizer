schema_version = 1
owner = "literalize-call"

[call]
target_function = "process"
parameter_names = ["values", "count"]
per_element = true
variant_only = true

[[call.variants]]
axis = "empty_container_type_hint"
