class Fixture_comment_terminators_scalar_Haxe_comment_terminator_block_scalar {
    public static function main() {
        /* inline scalar: * / |# -} *) (* ) =# ]] %} ]# % #> */
        final my_data = /* before scalar: * / |# -} *) (* ) =# ]] %} ]# % #> */
        "value";
    }
}
