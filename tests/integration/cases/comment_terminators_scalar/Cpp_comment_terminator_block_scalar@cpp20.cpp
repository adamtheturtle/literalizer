#include <initializer_list>
#include <string>
int main() {
/* inline scalar: * / |# -} *) (* ) =# ]] %} ]# % #> */
const auto* my_data = /* before scalar: * / |# -} *) (* ) =# ]] %} ]# % #> */
"value";
    (void)my_data;
    return 0;
}
