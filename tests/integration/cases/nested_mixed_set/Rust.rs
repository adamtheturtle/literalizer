use std::collections::HashMap;
use std::collections::HashSet;
fn main() {
    let my_data = HashMap::from([
        ("name", "Alice"),
        ("tags", "[\"42\", \"True\", \"apple\"]"),
    ]);
    let _ = my_data;
}
