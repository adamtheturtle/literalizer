let my_data = new Set([
  "apple",  // inline comment
  // before banana
  "banana",
  // trailing
]);
my_data = new Set([
  "apple",  // inline comment
  // before banana
  "banana",
  // trailing
]);
