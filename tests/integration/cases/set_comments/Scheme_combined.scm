(define my_data (list
    "apple"  ; inline comment
    ; before banana
    "banana"
    ; trailing
))
(set! my_data (list
    "apple"  ; inline comment
    ; before banana
    "banana"
    ; trailing
))
