schema_version = 1
owner = "literalize-call"

[call]
target_function = "Playlist.new"
parameter_names = ["x"]
per_element = true
wrap_in_file = true
languages = ["Crystal"]
languages_reason = "Crystal-specific: its constructor call target is the only spelling in the corpus whose components a wrapped file declares as a constant rather than as a method."
