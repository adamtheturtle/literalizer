schema_version = 1
languages = ["Kotlin"]
languages_reason = "Kotlin-specific: `intArrayOf` is an `IntArray`, unrelated to `Array<Int>`, so a map value carrying one needs the primitive array type."
suites = ["base"]

[[variants]]
axis = "sequence"
