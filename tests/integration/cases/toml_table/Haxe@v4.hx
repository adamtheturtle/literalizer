class Fixture_toml_table_Haxe {
    public static function main() {
        final my_data = ([
            "section" => (["value" => 1] : Map<String, Dynamic>),
        ] : Map<String, Dynamic>);
    }
}
