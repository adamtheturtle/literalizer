schema_version = 1
owner = "literalize-ref"
gates = [{ kind = "record_variant", variant = "field_type_split" }]

[ref]
gates = [{ kind = "record_variant", variant = "field_type_split" }]
heterogeneous_strategy = "RECORD"

[ref.value_sources]
first = '{"x": 1}'
