schema_version = 1
languages = ["TypeScript"]
languages_reason = "TypeScript is the only back end whose default map rendering is a bare object literal that the compiler infers as a closed type with no index signature; every other typed back end writes the map type into the literal."
suites = ["base"]

[[variants]]
axis = "type_hints"

[[variants]]
axis = "dict_format"

[[variants]]
axis = "language_version"
