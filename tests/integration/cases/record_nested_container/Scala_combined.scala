object Fixture_record_nested_container_Scala_combined {
var my_data = Map(
    "title" -> "report",
    "tags" -> List[String]("draft", "urgent", "review"),
    "priority" -> 2,
)
my_data = Map(
    "title" -> "report",
    "tags" -> List[String]("draft", "urgent", "review"),
    "priority" -> 2,
)
}
