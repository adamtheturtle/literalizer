use std::collections::HashMap;
use std::collections::HashSet;
fn main() {
    let _ = HashMap::from([
        ("name", "Alice"),
        ("age", 30),
        ("active", true),
        ("score", None),
    ]);
}
