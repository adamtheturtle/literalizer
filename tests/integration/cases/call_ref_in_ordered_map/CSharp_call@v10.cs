using System;
using System.Collections.Generic;
class Check {
static object process(object a = null) => null;
    public static void Main() {
var big_list = ValueTuple.Create(
    "x"
);
process(new Dictionary<string, object> {["m"] = big_list});
    }
}
