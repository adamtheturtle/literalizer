Imports System.Collections.Generic
Module Check
    Private Function process(ParamArray a() As Variant) As Variant
    End Function
    process(value:="hello")
    process(value:=42)
    process(value:=True)
End Module
