schema_version = 1
suites = ["base"]
languages = ["Python"]
languages_reason = "Sampled: whether a comment holding anchor and alias markers is mistaken for an alias is a parser concern, not a rendering one."
