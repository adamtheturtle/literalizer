schema_version = 1
languages = ["D"]
languages_reason = "Sampled: D represents subnormal handling while the float-format axis covers formatter choices for every language."
suites = ["base"]

[[variants]]
axis = "float_format"
