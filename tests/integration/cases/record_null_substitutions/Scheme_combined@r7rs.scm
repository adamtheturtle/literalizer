(define my_data (list
    (list (cons "replacement" '()) (cons "present" 1))
    (list (cons "replacement" 2) (cons "present" 3))
))
(set! my_data (list
    (list (cons "replacement" '()) (cons "present" 1))
    (list (cons "replacement" 2) (cons "present" 3))
))
