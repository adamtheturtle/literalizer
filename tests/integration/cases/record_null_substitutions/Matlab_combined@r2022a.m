my_data = struct(
    'rows', {{struct('replacement', [], 'present', 1), struct('replacement', 2, 'present', 3)}}
);
my_data = struct(
    'rows', {{struct('replacement', [], 'present', 1), struct('replacement', 2, 'present', 3)}}
);
