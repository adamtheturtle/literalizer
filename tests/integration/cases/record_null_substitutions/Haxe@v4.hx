class Fixture_record_null_substitutions_Haxe {
    public static function main() {
        final my_data = ([
            (["replacement" => -1, "present" => 1] : Map<String, Dynamic>),
            (["replacement" => 2, "present" => 3] : Map<String, Dynamic>),
        ] : Array<Dynamic>);
    }
}
