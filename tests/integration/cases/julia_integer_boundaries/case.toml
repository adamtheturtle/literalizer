schema_version = 1
languages = ["Julia"]
languages_reason = "Found in Julia; whether the integer boundary is Julia-specific is undecided (#3822)."
suites = ["base"]

[[variants]]
axis = "integer_format"
