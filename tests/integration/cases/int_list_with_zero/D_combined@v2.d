import std.json;
void main() {
auto my_data = JSONValue([
    JSONValue(0),
    JSONValue(1),
    JSONValue(-1),
]);
my_data = JSONValue([
    JSONValue(0),
    JSONValue(1),
    JSONValue(-1),
]);
}
