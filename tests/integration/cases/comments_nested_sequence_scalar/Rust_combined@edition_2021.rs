fn main() {
    let mut my_data = vec![
        vec!["ADD", "alice", "hello"],
        vec!["DEL", "bob", "5"],  // removes "world"
    ];
    my_data = vec![
        vec!["ADD", "alice", "hello"],
        vec!["DEL", "bob", "5"],  // removes "world"
    ];
    let _ = my_data;
}
