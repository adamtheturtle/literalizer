use std::collections::HashMap;
fn main() {
    let mut my_data = vec![
        vec![HashMap::from([("$ref", "my_str")])],
    ];
    my_data = vec![
        vec![HashMap::from([("$ref", "my_str")])],
    ];
    let _ = my_data;
}
