class Fixture_orderedmap_with_annotated_value_Haxe {
    public static function main() {
        final my_data = ([
            "a" => ([] : Array<Dynamic>),
            "b" => 1,
        ] : Map<String, Dynamic>);
    }
}
