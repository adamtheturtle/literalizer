(define my_data (list
    ; before
    (cons "answer" 42)  ; inline
    (cons "plain" "ok")
    ; trailing
))
(set! my_data (list
    ; before
    (cons "answer" 42)  ; inline
    (cons "plain" "ok")
    ; trailing
))
